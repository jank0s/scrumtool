<h2>Users</h2>
<div class="panel panel-default">
    <div class="panel-heading">
        <h5 class="pull-left">User list</h5>
        <%= button_to "Add user", new_user_path, :class => "btn btn-primary pull-right", :method => :get  %>
        <div class="clear"></div>
    </div>

    <!-- Table -->
    <table class="table">
        <thead>
          <tr>
            <th>ID</th>
            <th>Name</th>
            <th>Username</th>
            <th>E-Mail</th>
            <th>Role</th>
        </tr>
        </thead>
        <tbody>
            <% @users.each do |user| %>
                <tr>
                    <td><%= user.id %></td>
                    <td><%= user.name %> <%= user.surname %></td>
                    <td><%= user.username %></td>
                    <td><%= user.email %></td>
                    <td><%= user.role.name %></td>
                </tr>
            <% end %>
        </tbody>
    </table>
</div>